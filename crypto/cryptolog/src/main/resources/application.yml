server:
  port: 9200
spring:
  datasource:
    url: jdbc:mysql://localhost/cryptolog?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: cryptolog.404@gmail.com
    password: kyudwennokkhytll
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com
    protocol: smtp
    test-connection: false



    from:
     email: cryptolog.404@gmail.com

  queries:
    users-query: select email, password, '1' as enabled from auth_user where email=? and verified='true'
    roles-query: select u.email, r.role_name from auth_user u inner join auth_user_role ur on(u.auth_user_id=ur.auth_user_id) inner join auth_role r on(ur.auth_role_id=r.auth_role_id) where u.email=?
security:
  basic:
    enabled: false

#  queries:
#    users-query: select email, password, '1' as enabled from auth_user where email=? and status='VERIFIED'
#    roles-query: select u.email, r.role_name from auth_user u inner join auth_user_role ur on(u.auth_user_id=ur.auth_user_id) inner join auth_role r on(ur.auth_role_id=r.auth_role_id) where u.email=?
#security:
#  basic:
#    enabled: false


